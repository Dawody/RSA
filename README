Dependences :
1. python
2. pycrypto # install it by pip ($sudo pip install pycrypto) --i used it to generate pig prime numbers



Execution :
after installing the dependences; just run the following command
$python RSA.py
